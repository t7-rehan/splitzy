# Environment (never commit real credentials)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log
npm-debug.log*

# Prisma local artifacts
prisma/*.db

# Portable local PostgreSQL binaries + data (Task 7) — never commit
.postgres-local/

# OS / editor noise
.DS_Store
Thumbs.db
